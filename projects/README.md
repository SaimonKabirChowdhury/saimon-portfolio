# Project Archive

This directory is Saimon’s build archive. Each numbered folder is intentionally ready for its own source code, experiments, assets, documentation and deployment configuration.

Start a project by replacing its short placeholder README with its product brief, then add the implementation inside the same folder. SwiftRide is the featured UX case study in the portfolio website.

